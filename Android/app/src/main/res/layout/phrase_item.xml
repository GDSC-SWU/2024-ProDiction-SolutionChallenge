<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingTop="20dp">
    <TextView
        android:id="@+id/ic_phrase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔗"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
    <TextView
        android:id="@+id/tv_phrase_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ㄱ"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/font"
        app:layout_constraintTop_toTopOf="@+id/ic_phrase"
        app:layout_constraintBottom_toBottomOf="@+id/ic_phrase"
        app:layout_constraintStart_toEndOf="@+id/ic_phrase"
        android:layout_marginStart="5dp"/>
    <ImageButton
        android:id="@+id/btn_phrase_right"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:src="@drawable/ic_word_right"
        android:background="#00ff0000"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toTopOf="@+id/ic_phrase"
        app:layout_constraintBottom_toBottomOf="@+id/ic_phrase"
        app:layout_constraintStart_toEndOf="@+id/tv_phrase_main"
        android:layout_marginStart="5dp"/>

    <Button
        android:id="@+id/btn_phrase1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="가는 기차"
        android:background="@drawable/btn_background_round_border"
        android:textColor="@color/main"
        android:textSize="15sp"
        android:textAlignment="textStart"
        android:paddingVertical="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ic_phrase"
        android:layout_marginTop="15dp"
        android:paddingHorizontal="15dp"/>
    <Button
        android:id="@+id/btn_phrase2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="귀여운 강아지"
        android:background="@drawable/btn_background_round_border"
        android:textColor="@color/main"
        android:textSize="15sp"
        android:textAlignment="textStart"
        android:paddingVertical="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btn_phrase1"
        android:layout_marginTop="10dp"
        android:paddingHorizontal="15dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>