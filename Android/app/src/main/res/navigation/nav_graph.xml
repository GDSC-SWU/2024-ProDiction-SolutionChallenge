<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/permissions_fragment">

    <fragment
        android:id="@+id/permissions_fragment"
        android:name="com.example.pro_diction.PermissionsFragment"
        android:label="PermissionsFragment">

        <action
            android:id="@+id/action_permissions_to_comm"
            app:destination="@id/comm_fragment"
            app:popUpTo="@id/permissions_fragment"
            app:popUpToInclusive="true" />

    </fragment>

    <fragment
        android:id="@+id/comm_fragment"
        android:name="com.example.pro_diction.CommFragment"
        android:label="CommFragment">

        <action
            android:id="@+id/action_comm_to_permissions"
            app:destination="@id/permissions_fragment"
            app:popUpTo="@id/comm_fragment"
            app:popUpToInclusive="true" />
    </fragment>

    <!--
    <fragment
        android:id="@+id/gallery_fragment"
        android:name="com.example.pro_diction.gesturerecognizer.fragment.GalleryFragment"
        android:label="GalleryFragment" /> -->
</navigation>